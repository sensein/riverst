# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# package-lock.json is intentionally tracked: without it, npm resolves
# transitive peer deps afresh on every build, which silently broke the
# settings form (@rjsf/utils 6.4.2 -> 6.10.1). demo-site tracks its
# lockfile too.
tsconfig.tsbuildinfo
.env
