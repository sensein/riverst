{
  "name": "riverst-e2e",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node capabilities.test.mjs",
    "setup": "npm install && npx playwright install chromium"
  },
  "devDependencies": {
    "playwright": "1.63.0"
  }
}
